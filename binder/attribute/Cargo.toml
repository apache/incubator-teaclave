[package]
name = "teaclave_binder_attribute"
version = "0.2.0"
authors = ["root"]
edition = "2018"

[lib]
proc-macro = true

[dependencies]
quote = "0.6"
syn = { version = "0.15", features = ["full"] }
